#include "main.h"

/**
  * main -program that prints its name
  * @argc: argument count as interger
  * @argv: string values of argument
  * Return: 0
  */

int main(int argc, char *argv[])
{
	UNUSED(argc);
	printf("%s\n", argv[0]);
	return (0);
}
